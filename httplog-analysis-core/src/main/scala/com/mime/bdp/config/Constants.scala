package com.mime.bdp.config

object Constants {
  val KEY_KAFKA_BROKER = "broker"
  val KEY_KAFKA_TOPICS = "topics"
  val KEY_KAFKA_SCHEMA = "schema"

  val KEY_GROUP_BY = "group_by"
  val KEY_EVENT_TIME = "event_time"
  val KEY_EVENT_NAME = "event_name"
  val KEY_EVENT_LIST = "event_list"
  val KEY_TABLES = "tables"

}
